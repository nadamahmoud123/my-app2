name: react application

on:
  push:
   branches: [ main ]
  pull_request:
    branches: [ main ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "nadamahmoud123" --password-stdin
        docker image build -t nadamahmoud123/pyflask:latest .
        docker push nadamahmoud123/pyflask:latest
